import React from 'react';
import "../styles/header.css";

function Header () {
    return (
        <header className = "header">
            <h1 className = "title">Company X</h1>
            <h2 className = "subtitle">Employee Directory</h2>
        </header>
    );
}

export default Header;
